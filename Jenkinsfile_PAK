pipeline {
    agent {
        docker { image 'pakhoa/robot_ubuntu' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'cd Demo && ls -l'
                sh 'rcc run'
            }
        }
    }
}
