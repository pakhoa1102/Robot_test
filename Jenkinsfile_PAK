pipeline {
    agent {
        docker { image 'pakhoa/robot_ubuntu' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'rcc run -r Container/robot.yaml'
            }
        }
    }
}
