pipeline {
    agent {
        docker { image 'pakhoa/robot_ubuntu' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'cd Demo'
                sh 'rcc run'
            }
        }
    }
}
